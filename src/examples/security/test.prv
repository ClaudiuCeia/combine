match foo {
    function isOverAge() {
        if (4 * 6 % 3 * 0 < 2 * 2000 - 5 + 20) {
            let a = 2, x = 1 + 3;
            let b = 5 - 2 * 3;
            return a;
        }
    }

            
    allow read: true;
}